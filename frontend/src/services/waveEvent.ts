// Wave ripple effects removed as per user instruction (only character hover responses remain)
export const triggerButtonWave = (_clientX?: number, _clientY?: number, _power?: number) => {
  // No-op: ripples removed
};
